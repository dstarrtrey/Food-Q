type User {
  id: ID! 
  username: String!
  password: String!
}

type MenuItem {
  id: ID!
  name: String!
  cost: Int
  inStock: Boolean
}

type WaitListItem {
  id: ID! 
  name: String!
  partySize: Int!
  phoneNumber: Int
}

type Mutation {
  hi: String
}

type Query {
  users: [User]!
}
